import styled from 'styled-components';

const TodoContentText = styled.span`
  font-size: 1.1rem;
  font-weight: 500;
  width: 100%;

  &:hover {
    color: #aa00cc;
  }
`;

export default TodoContentText;
