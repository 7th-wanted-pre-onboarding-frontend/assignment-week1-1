import styled from 'styled-components';

const TodoFunction = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: white;
  border-radius: 5px;
`;

export default TodoFunction;
